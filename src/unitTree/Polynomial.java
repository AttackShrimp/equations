package unitTree;

import java.util.List;

public class Polynomial extends Operable {
    List<PolynomialLevel> levels;

    public Polynomial(List<Link> links) {
        // links should only have one value, represent as a linked list where a vertex holds an operation
    }
}
